#include <stdio.h>
#include "main.h"

/**
 * main - a function that prints the alphabet,
 * in lowercase, followed by a new line.
 *
 * Return: 1 means success
 */

int main(void)
{
	print_alphabet();

	return (0);
}
