#include "main.h"
#include <ctype.h>

/**
 * mul - a function that multiplies two integers.
 *
 * @a: arg 1 for mul function
 * @b: arg 2 for mul function
 * Return: 1 true 0 means false
 */
int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
