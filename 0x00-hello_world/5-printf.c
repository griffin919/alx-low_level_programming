#include <stdio.h>
/**
 * main - outputs string with special chars
 * Return: 0 means success
 */
int main(void)
{
	char message[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", message);

	return (0);
}

